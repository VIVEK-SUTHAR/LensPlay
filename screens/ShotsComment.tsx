import React from "react";
import { RootStackScreenProps } from "../types/navigation/types";

export default function ShotsComment({
  navigation,
}: RootStackScreenProps<"ShotsComment">) {
  return <div>ShotsComment</div>;
}
